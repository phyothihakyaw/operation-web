# Base URL of the LearnWU API, without a trailing slash.
# Dev:        https://api.dev.learnwu.com
# Leave empty to run the app fully on built-in sample data (no backend needed).
NEXT_PUBLIC_API_URL=
